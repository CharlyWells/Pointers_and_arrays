#include <stdio.h>

/**
 * main - addresses of variabes
 * return - Always 0
 */
int main(void)
{
   int a;
   int b;
   printf("The address of a is: %p\n",a);
   printf("the address of b is: %p\n",b);
   return (0);

}
